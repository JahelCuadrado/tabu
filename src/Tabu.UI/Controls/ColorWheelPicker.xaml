<UserControl x:Class="Tabu.UI.Controls.ColorWheelPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="False">
    <UserControl.Resources>
        <!-- Vertical brightness slider: minimal track + circular thumb,
             overrides the app-wide horizontal-only Slider style. -->
        <Style x:Key="BrightnessSliderStyle" TargetType="Slider">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Width="6" CornerRadius="3"
                                    Background="{DynamicResource TertiaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    HorizontalAlignment="Center" />
                            <Track x:Name="PART_Track" Orientation="Vertical" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge" Opacity="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="16" Height="16" Cursor="Hand">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="{DynamicResource PrimaryTextBrush}"
                                                         Stroke="{DynamicResource PrimaryBackgroundBrush}"
                                                         StrokeThickness="2" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- HSV wheel: bitmap + selector overlay. -->
        <Grid x:Name="WheelHost" Grid.Column="0"
              Width="140" Height="140"
              Background="Transparent"
              MouseLeftButtonDown="Wheel_MouseDown"
              MouseLeftButtonUp="Wheel_MouseUp"
              MouseMove="Wheel_MouseMove">
            <Image x:Name="WheelImage" Stretch="None" />
            <Canvas IsHitTestVisible="False">
                <Ellipse x:Name="WheelSelector"
                         Width="12" Height="12"
                         Stroke="White" StrokeThickness="2"
                         Fill="Transparent">
                    <Ellipse.Effect>
                        <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.6" Color="Black" />
                    </Ellipse.Effect>
                </Ellipse>
            </Canvas>
        </Grid>

        <!-- Brightness (Value) slider, vertical. -->
        <Slider x:Name="BrightnessSlider" Grid.Column="1"
                Style="{StaticResource BrightnessSliderStyle}"
                Orientation="Vertical"
                Minimum="0" Maximum="1"
                Value="1"
                Width="20" Height="140"
                Margin="14,0,0,0"
                IsSnapToTickEnabled="False"
                ValueChanged="Brightness_Changed" />
    </Grid>
</UserControl>
